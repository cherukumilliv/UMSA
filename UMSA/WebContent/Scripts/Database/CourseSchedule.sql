create table CourseSchedule(
	courseNumber varchar(10),
	termid	varchar(10),
	sectionId varchar(10),
	startdate	datetime,
	enddate	datetime,
	instructionMode varchar(25),
	roomId varchar(25),
	professorId carchar(10)
)